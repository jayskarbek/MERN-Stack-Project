# Copy this file to `.env` in the same directory and set the values.

# Base URL of the existing Express backend. Do not include a trailing slash.
API_BASE_URL=http://localhost:5000/api

# When testing against a deployed backend, update to the public URL, e.g.:
# API_BASE_URL=https://your-domain/api
